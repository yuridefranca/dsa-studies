import { describe, it, expect } from 'vitest';
import { {functionName} } from './index';

describe('{Problem Title}', () => {
  it('example 1', () => {
    expect({functionName}({input1})).toEqual({output1});
  });

  it('example 2', () => {
    expect({functionName}({input2})).toEqual({output2});
  });

  // Add additional test cases from exampleTestcases if available
});
